PORT=3000

# Database (از همروش)
DB_HOST=eventio.royaghavami.svc.cluster.local
DB_PORT=3306
DB_USER=root
DB_PASSWORD=cLNC6XlB1lckcFSu8A8d
DB_NAME=eventio
DB_SYNCHRONIZE=true
# Auth
JWT_SECRET=super-strong-secret-change-this

# Frontend domain(s) — comma-separated (NOT the API URL)
CORS_ORIGIN=https://eventio.ir

# App behavior
EVENT_REQUIRE_ADMIN_APPROVAL=false

# Node environment
NODE_ENV=production